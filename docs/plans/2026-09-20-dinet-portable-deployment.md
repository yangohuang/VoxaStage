# 已验证DINet部署的可复现入口

沿用用户已授权且完成实测的256人物、0.4秒消费提前量与有界代理修复，将本机启动脚本整理到源码；不新增模型或修改生成策略。

1. 配置测试先行：固定版本、256宽度、合法人物ID、绝对资源路径、文件/目录类型与输入大小；错误在原生导入前拒绝。
2. 人物选择为worker启动器可选参数。不传时保留原设置。工厂源码版本固定在代码，不允许配置绕过指纹。检查及编译完成后才应用设置。
3. 代理启动器接受原生根目录和端口，复用已经验证的有界等待与先取消任务再关闭连接行为；不复制原生源码，原生版本不匹配即退出。
4. CPU回归、归档原函数的背压/清理测试，以及相同真实服务上的输出复验。发布文件使用固定快照，保留旧启动命令以回退。
5. 文档明确上游资源、进程重启与容器重建的边界，不声称空白GPU部署或人工音画质量已验收。
