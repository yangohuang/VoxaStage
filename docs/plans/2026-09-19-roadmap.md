# 数字人与多模态工程路线

## 目标与阶段门槛

以现有三种数字人和两条语音链路为基础，形成可复现的评测、优化和多模态交互成果。基础模型归属保持清楚，所有改善必须有基线与对照。

1. **基线**：统一三驱动的输入清单、校验、重复运行和 JSON / Markdown 报告。先验收驱动适配层，再扩展到语音链路和浏览器。质量评估与传输同步分开；未测项目显式为空。不能用合成语音代替真实麦克风结论。
2. **优化**：根据基线选择一个实测瓶颈，在相同输入、相同环境与运行模式下对照，报告质量、延迟、资源代价。未证明改善的方案不替换默认。
3. **视觉交互**：视觉输入带采集时间、轮次和有界历史，支持当前画面与前一画面指代；以图像单模态、语音单模态和联合输入比较。先建立服务能力检查，不能假定已部署语音模型启用了视觉路径。
4. **针对性实验**：从失败样本选择一个问题，比较基线与方法变体。推理策略实验与训练实验明确区分；提供复现说明、消融和演示。

不同时展开多个未经验证的模型改动。每阶段保留通过/失败样本、版本、输入哈希和测量口径。仓库继续私有，运行素材与模型权重不随源码导出。

## 第一阶段设计：统一驱动基线

优先复用现有 Provider 适配器，新增独立评测入口，不修改在线服务与前端。替代方案是直接依赖各模型原生脚本（测量口径不统一）或立即加入全量浏览器测量（变量过多）；先建立可重复的适配层基线，后续分层补充。

- 输入：部署者提供 JSON manifest，含 case id、WAV 路径、SHA256、类别、来源、转写及人工核对状态。校验单声道 PCM16、8–48kHz、0–30秒。记录原始文件和24k播放 PCM 哈希；清单不含音色或模型密钥。
- 执行：选定一个 provider，串行运行；预热单独记录、不计入正式统计。分别支持快速送入和实时节奏；这两种模式不混算。生产适配器原有节奏控制仍生效，不将wall/audio比值称为裸模型RTF。
- 指标：首配对媒体到达时间、整段耗时、wall/audio比值、收帧间隔、PCM逐样本保持、帧序号、起始样本与PTS一致性、完整结束与取消后恢复。
- 报告：JSON逐次记录与Markdown汇总，按provider和case分组，统计成功数、失败数、样本量、P50/P95（线性插值）；失败不进入成功延迟分位数，但保留失败分母。
- 测量边界：接收媒体不是浏览器播放；音频相等和PTS连续不代表口型准确。口型同步、身份保持、视觉伪影、真实设备回声初始标记为未评估，后续另设人工标注与模型评估。
- 错误：开跑前覆盖旧成功标记，逐次原子保存；单次失败留下报告并继续其他预定case，最终非零退出。取消必须关闭异步生成器；输出不记录后端URL或原始帧。

## 第一阶段实施计划

- [x] 新增 `avatar_eval_metrics.py` 与 `test_avatar_eval_metrics.py`：先测试不连续PCM、错PTS、缺结束、空流、NaN与失败统计，再实现事件检查与分位汇总。
- [x] 新增 `benchmark_avatar.py` 与 `test_benchmark_avatar.py`：manifest严格校验、WAV归一化、串行预热/正式轮次、时间记录、超时清理、取消恢复、原子写报告；用可控异步后端验证失败路径与执行顺序。
- [x] 新增 `docs/AVATAR-EVALUATION.md`：命令、清单示例、时钟口径、质量标注规则和阶段边界。
- [x] 更新源码导出白名单；确保不导出输入音频、URL、角色ID与机器配置。
- [x] 在当前有权使用的合成样本上对可用真实驱动做相同输入的串行初始基线；记录服务不可用而不伪造通过。初始样本集是smoke基线，不标作统计充分的质量数据集。
- [x] 独立需求与代码审查，运行新增测试和现有回归；把结果写入阶段记录后再开展瓶颈优化。

## 后续基线扩展

补充中文、英文、中英混合、静音、停顿样本与人工核对；补充浏览器播放起点与打断指标；区分预加载首请求和稳定重复请求。至少完成这些口径后，才声称第一阶段完整质量与端到端基线验收。

## 2026-09-19 检查点

适配层子任务通过，报告见[初始基线](../AVATAR-BASELINE-2026-09-19.md)：相同代码版本、相同三条合成输入，三驱动54次请求通过；96项应用及评测Python、6项worker、16项前端测试通过。

第一阶段整体仍在进行：下一步扩展五类样本、生成质量输出与浏览器端计时。未满足完整基线门槛之前，不进入性能优化结论或模型训练。

## 输出与浏览器检查点

- [x] 五类15条受控输入和45次真实输出捕获；3644帧与PCM一致。
- [x] 两后端×三驱动浏览器回复、手动打断、恢复和idle检查；修正测试的提前排空判定后6/6通过。
- [x] 保留失败记录、审查测量范围与异常清理，新增18项Python和6项浏览器测试通过。
- [ ] 完整动态质量复核、人工转写/试听、3D渲染、精确采集时间与语音打断。
- [ ] 固定输入重复验证DINet恢复请求的音频调度空隙，再设计优化对照。

详见[输出与浏览器记录](../AVATAR-OUTPUT-BASELINE-2026-09-19.md)。第一阶段仍为进行中，不将工具覆盖视作感知质量验收。

## 固定PCM复现检查点

- [x] 独立播放评测页复用生产播放器；三驱动同一PCM各3轮，最终12次请求（含预热与3D探针）通过。
- [x] DINet空隙重复复现，关联到第19→20帧的适配器出口间隔；浏览器JPEG解码不是该间隔的主要来源。
- [x] 3D语音、静音和停顿实际播放，完成多个时点的网格渲染抽查。
- [ ] 继续拆解DINet原生服务/适配器输入节奏/编码；完整动态质量、人工核对、精确采集与语音打断仍待补。

见[固定PCM结果](../FIXED-PLAYBACK-BASELINE.md)。仍不把协议与渲染通过等同于音素口型质量通过。

## DINet供帧归因检查点

- [x] 可选原生接收/编码/输入节奏观测，4次固定PCM请求通过。
- [x] 约250ms主要间隔在下一原生帧头交付之前；帧体和编码墙钟仅数毫秒/十几毫秒。
- [x] 只读确认原生消费者首次5块后的等待策略与边界吻合，记录文件摘要；未修改原生服务。
- [ ] 后续受控变量为保留有界输入提前量，同时检查长期缓冲、取消与输出质量。尚未声称因果干预或优化通过。

详见[供帧归因](../DINET-DELIVERY-ATTRIBUTION.md)。第一阶段剩余质量与语音打断门槛继续保留。

## 自动语音打断检查点

- [x] 固定16kPCM分包输入，两后端×三驱动最终6/6完成自动reset、旧音源停止、新回复播放和已观察片段排空/idle。
- [x] 排除手动打断与文本命令，记录每包样本边界；11项新增测试及独立审查通过。
- [x] 保存初始连接定位失败、级联reset原因误判失败；回答质量另记：级联严格“只说你好”文字约束3/3失败，端到端3/3通过。
- [ ] 完整动态质量与人工听写/试听、实际采集时间仍未完成；输入传输边界不替代物理麦克风测量。

见[语音打断实测](../VOICE-INTERRUPTION-BASELINE.md)。第一阶段仍在进行；此检查点没有提前验收性能优化、视觉输入或方法实验。

## 浏览器采集边界检查点

- [x] 默认关闭的worklet诊断，将输出PCM样本区间、输入处理帧与主线程观察关联；普通二进制传输保持兼容。
- [x] 真实文件虚拟麦克风发现启动处理块断点，先保存失败再实现分段映射；跨断点包明确无效，后续恢复，不改变音频。
- [x] 默认16k、诊断16k、诊断48k三次最终请求各260包，回调与接收端PCM SHA256一致；诊断各259包有效、1包跨断点无效。
- [x] 新增12项测试通过，独立审查核验多采样率、多断点与分数采样位置。
- [ ] 动态口型质量与人工听写/试听仍待补。浏览器采样位置已经可关联，物理设备声学边界不能由虚拟麦克风推断。

详见[采集边界记录](../CAPTURE-TIMING-BASELINE.md)。完整第一阶段状态保持进行中，下一步优先形成可复核的动态质量材料。

## 动态质量材料检查点

- [x] 离线复核入口读取45个已保存捕获，按原始音频与PTS重放2D/3D；匿名编号、逐帧回看和有界预载完成。
- [x] 明确复核来源、时间范围和维度，本地保存/重启恢复/JSON导出完成，未评估不填0。
- [x] 9段真实浏览器重放及快速切换通过，独立验证目录完成标注往返测试；15项服务、9项前端核心测试通过。
- [x] 需求与代码审查修复切换资源暂留、Unicode持久化问题；保留自动化启动与WebGL截图误判的失败证据。
- [ ] 实际质量观察与人工试听记录尚未完成，UI自动测试不作为质量评分。下一步使用这些材料形成真实质量结论，再验收第一阶段。

入口与边界见[动态质量复核](../AVATAR-QUALITY-REVIEW.md)。后续优化、视觉交互和方法实验目标不变。

## 有界提前量优化检查点

- [x] 隔离对照只改变等待提前量，三档交错检查，保留传输压缩失配的失败记录；一条固定输入的61帧JPEG/PCM完全一致。
- [x] 配置化启动器保留原代理与worker生命周期，固定原生版本校验，0秒回退，坏配置明确失败。
- [x] 原代理17次API检查及10次正式浏览器对照；0.4秒配置消除了本次测到的音频调度间隙，已在当前部署保留。
- [x] 独立到期回滚及浏览器工具故障的实际恢复均核验；文档与机器数据分别保留失败和通过记录。
- [ ] 人工口型/音色/动态质量、物理设备声学标定仍未验收，不能将该局部优化当作第一阶段完整质量通过。

详见[原代理集成](../DINET-PACING-INTEGRATION.md)。下一步按第三阶段设计检查本地模型的真实视觉能力，再实现带时间戳、轮次与有界历史的视觉语音交互；优化收益保持上述限定，不再扩大为整体模型质量结论。
